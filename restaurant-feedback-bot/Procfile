# Pulse bot + Mini App + consultant platform API on one Railway service.
# Required: BOT_TOKEN, ADMIN_IDS, DATABASE_URL.
# Platform login users are bootstrapped from PLATFORM_BOOTSTRAP_USERS_JSON.
worker: python launcher.py
